# Variables de entorno para el backend (copiar a .env y ajustar según entorno)

# Base de datos (dev por defecto: SQLite)
DATABASE_URL=sqlite:///./handover.db

# CORS (separadas por coma). Para dev, "*" permite todo.
CORS_ORIGINS=*

# Nivel de log: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO
